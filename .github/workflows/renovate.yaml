# Self-hosted Renovate. Reads /renovate.json5 from the cloned repo automatically.
name: Renovate

on:
  schedule:
    # Opens PRs. Non-majors arm GitHub's auto-merge and land on green; majors get a Copilot BC check.
    - cron: "13 5 * * *"
  workflow_dispatch:
    inputs:
      logLevel:
        description: Renovate log level
        default: info
        type: choice
        options: [info, debug]
      dryRun:
        description: Dry run (extract + plan, open no PRs)
        default: false
        type: boolean

jobs:
  renovate:
    uses: yama6a/gha/.github/workflows/renovate.yaml@v2
    permissions:
      contents: read # Renovate acts through RENOVATE_TOKEN, not GITHUB_TOKEN
      copilot-requests: write # the BC check's Copilot CLI is the one thing on GITHUB_TOKEN
    with:
      log-level: ${{ inputs.logLevel || 'info' }}
      dry-run: ${{ inputs.dryRun || false }}
    secrets:
      RENOVATE_TOKEN: ${{ secrets.RENOVATE_TOKEN }}
